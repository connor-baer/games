---
import Layout from '../../layouts/Wizard.astro';
import DealerForm from '../../components/wizard/DealerForm.svelte';
import { t } from '../../utils/i18n';

const { title, description } = t.wizard.dealer;
---

<Layout title={title} description={description}>
  <DealerForm client:load />
</Layout>
